apply plugin: 'java-library'

group = 'at.ofai.music'
version = '0.5.8-custom'

ext.moduleName = 'intervalmusiccompositor.beatroot'

dependencies {
  compile project(':intervalmusiccompositor.spi')
  testCompile project(':intervalmusiccompositor.decoder.mpeg')
}
